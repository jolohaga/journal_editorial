<tr>
  <td style='width: 20%;'><%= link_to form_letter.name, send("#{@notifiable.class.name.downcase}_notification_path",@notifiable,form_letter) %></td>
  <td><%= truncate(ERB.new(form_letter.body).result(binding),:length => 320) %></td>
</tr>