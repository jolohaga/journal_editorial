<%= form_for @correspondence, correspondences_path do |f| %>
  <% if @correspondence.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@correspondence.errors.count, "error") %> prohibited this role from being saved:</h2>

      <ul>
      <% @correspondence.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :from %><br />
    <%= f.text_field :from, :value => current_user.name %>
  </div>
  <div class="field">
    <%= f.label :on_behalf_of %><br />
    <%= f.text_field :on_behalf_of, :value => current_user.name %>
  </div>
  <div class="field">
    <%= f.label :subject %><br />
    <%= f.text_field :subject %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit 'Create web submission' %>
  </div>
<% end %>